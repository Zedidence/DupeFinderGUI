# Core dependencies
Pillow>=9.0.0
imagehash>=4.0.0
flask>=2.0.0
numpy>=1.20.0    # Required by imagehash
pillow-heif>=0.10.0  # HEIC/HEIF format support

# Optional but recommended
tqdm>=4.0.0      # Progress bars